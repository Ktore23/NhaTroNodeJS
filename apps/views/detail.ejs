<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> - Chi Tiết Nhà Trọ</title>
    <link rel="stylesheet" href="/static/css/styles.css">
    <link rel="stylesheet" href="/static/css/map.css">
    <link rel="stylesheet" href="/static/css/detail.css">
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" integrity="sha256-p4NxAoJBhIIN+hmNHrzRCf9tD/miZyoHS5obTRR9BMY=" crossorigin="" />
</head>
<body>
    <%- include('partical/menu') %>

    <div class="detail-page">
        <div class="post-details">
            <h2 id="post-title"><%= post.title %></h2>
            <div class="post-images">
                <% if (post.images && post.images.length > 0) { %>
                    <% post.images.forEach((image, index) => { %>
                        <img src="<%= image %>" alt="<%= post.title %>" class="<%= index === 0 ? 'active' : '' %>">
                    <% }) %>
                <% } else { %>
                    <img src="https://via.placeholder.com/300x150" alt="Không có hình ảnh" class="active">
                <% } %>
                <% if (post.images && post.images.length > 1) { %>
                    <div class="carousel-controls">
                        <button class="carousel-btn" onclick="prevImage()">❮</button>
                        <button class="carousel-btn" onclick="nextImage()">❯</button>
                    </div>
                <% } %>
            </div>
            <p><strong>Khu vực:</strong> <span id="post-location"></span></p>
            <p><strong>Giá:</strong> <span id="post-price"></span></p>
            <p><strong>Diện tích:</strong> <span id="post-area"></span></p>
            <p><strong>Mô tả:</strong> <span id="post-description"></span></p>
            <p><strong>Tiện ích:</strong> <span id="post-amenities"></span></p>
            <p><strong>Người đăng:</strong> <span id="post-username"></span></p>
            <p><strong>Email liên hệ:</strong> <span id="post-email"></span></p>
            <p><strong>Ngày đăng:</strong> <span id="post-createdAt"></span></p>
        </div>
        <div class="map-container">
            <div id="map" 
                 data-lat="<%= post.coordinates && post.coordinates.latitude ? post.coordinates.latitude : '10.7769' %>"
                 data-lng="<%= post.coordinates && post.coordinates.longitude ? post.coordinates.longitude : '106.7009' %>"
                 data-title="<%= post.title %>"
                 data-location="<%= post.location %>"></div>
        </div>
    </div>

    <div class="footer">
        <p>© 2025 Thuê Nhà Trọ. All rights reserved.</p>
    </div>

    <script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js" integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo=" crossorigin=""></script>
    <script src="/static/js/map.js"></script>
    <script src="/static/js/detailapi.js"></script>
    <script src="/static/js/image.js"></script>
</body>
</html>